name: Release

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

# softprops/action-gh-release needs write access; the default GITHUB_TOKEN is read-only.
permissions:
  contents: write

jobs:
  # One job creates the release; the platform jobs only ever upload into it. Before this each of
  # the four raced `action-gh-release`, and on v0.12.0-alpha.1 two of them created one release
  # object while two created another, splitting the artifacts across a published release and a
  # draft — and failing the macOS job when it tried to publish the loser.
  create-release:
    name: Create the release
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The tag's own CHANGELOG section becomes the release body. Empty means the section is
      # missing, which is worth failing the release over — a release with no notes is the state
      # this repo shipped eight versions in.
      - name: Extract release notes
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v v="$VERSION" '
            $0 ~ "^## " v " " { want = 1; next }
            want && /^## / { exit }
            want { print }
          ' CHANGELOG.md > notes.md
          test -s notes.md || { echo "::error::no CHANGELOG.md section for $VERSION"; exit 1; }
          cat notes.md

      - name: Create it
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          # Re-running a release workflow is a normal thing to do; the release surviving from the
          # first attempt is not an error.
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "$TAG already exists"
            exit 0
          fi
          # A hyphen in the tag is a prerelease (`v0.12.0-alpha.1`): flagged as one, and never
          # made "Latest", so nobody lands on an alpha from the repo's front page.
          case "$TAG" in
            *-*) flags="--prerelease --latest=false";;
            *)   flags="--latest";;
          esac
          gh release create "$TAG" --title "$TAG" --notes-file notes.md $flags

  appimage:
    name: Linux AppImage
    # 22.04 sets the glibc floor (2.35); an AppImage built on newer won't run on older distros.
    runs-on: ubuntu-22.04
    # Waits only so the release exists before the upload at the end. `!cancelled()` because on a
    # push to main `create-release` is skipped, and a skipped dependency would otherwise skip the
    # build too.
    needs: create-release
    if: ${{ !cancelled() }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      # Same build deps as CI; plus FUSE isn't needed (the script uses extract-and-run).
      - name: Install Linux deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libudev-dev libxkbcommon-dev \
            libwayland-dev libxcb1-dev libgtk-3-dev

      - name: Build AppImage
        run: bash packaging/build-appimage.sh

      - name: Smoke-test the AppImage
        run: |
          APPIMAGE_EXTRACT_AND_RUN=1 ./dist/HookEcho-x86_64.AppImage \
            --headless-icon /tmp/icon.png
          test -s /tmp/icon.png

      # The .deb is built on the same runner: it wants the same glibc floor, the same release
      # binary, and `$auto` resolves its dependencies against whatever libs this image carries.
      - name: Install cargo-deb
        run: cargo install cargo-deb --locked --version ^3.7

      - name: Build .deb
        run: |
          bash packaging/build-deb.sh
          cp target/debian/*.deb dist/
          # A second copy under a name that does not move, kept out of `dist/` so the tagged
          # upload's `dist/*.deb` still means exactly one versioned file. The rolling release
          # uploads this one, so each push replaces the previous build instead of adding to a pile.
          mkdir -p dist/rolling
          cp target/debian/*.deb dist/rolling/HookEcho-amd64.deb
          dpkg -c dist/*.deb

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HookEcho-x86_64.AppImage
          path: dist/HookEcho-x86_64.AppImage

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: hookecho-deb
          path: dist/*.deb

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: gh release upload "$TAG" dist/HookEcho-x86_64.AppImage dist/*.deb --clobber

      # Every push to main refreshes a rolling "latest" prerelease, so the Releases page always
      # carries current builds. Before this, artifacts only ever reached Releases on a tag push,
      # and 49 commits' worth of green runs published nothing.
      - name: Update rolling release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build (main)
          prerelease: false
          make_latest: true
          body: |
            Rolling build of `main` — rebuilt and re-published on every push, so this
            is always the newest code. Older releases are kept as drafts.
          files: |
            dist/HookEcho-x86_64.AppImage
            dist/rolling/HookEcho-amd64.deb

      # The rolling release is supposed to hold one build, and it held eighteen assets: seven
      # .debs going back to 0.8.0, because `cargo-deb` puts the version in the filename and
      # `--clobber` cannot clobber a name that changes every release. A stable name for the
      # rolling copy fixes that going forward; the step below clears what has already piled up.
      #
      # Tagged releases keep the versioned filename, which is correct — a tag *is* a version.
      - name: Prune stale rolling assets
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Two patterns, both provably dead, so this can never delete a live asset:
          #   hookecho_<version>_amd64.deb   the versioned debs, superseded by HookEcho-amd64.deb
          #   Hook_Echo-WX-*                 the old product name, last built 2026-08-26
          gh release view latest --json assets -q '.assets[].name' \
            | grep -E '^(hookecho_.*_amd64\.deb|Hook_Echo-WX-.*)$' \
            | while read -r name; do
                echo "removing stale asset $name"
                gh release delete-asset latest "$name" --yes
              done

  windows:
    name: Windows zip
    runs-on: windows-latest
    # Waits only so the release exists before the upload at the end. `!cancelled()` because on a
    # push to main `create-release` is skipped, and a skipped dependency would otherwise skip the
    # build too.
    needs: create-release
    if: ${{ !cancelled() }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release -p hookecho

      - name: Smoke-test
        shell: bash
        run: |
          ./target/release/hookecho.exe --headless-icon icon.png
          test -s icon.png

      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          cp target/release/hookecho.exe dist/
          cd dist && 7z a hookecho-windows-x86_64.zip hookecho.exe

      # Two versions, because the installers cannot take the one the tag uses: WiX rejects
      # anything but `x.x.x.x` ("The Product/@Version attribute's value, '0.12.0-alpha.1', is not
      # a valid version"), and Inno is no happier. `numeric` drops the prerelease suffix, so
      # `0.12.0-alpha.1` installs as 0.12.0 — the release page still carries the full name.
      - name: Read version
        id: ver
        shell: bash
        run: |
          version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "numeric=${version%%-*}" >> "$GITHUB_OUTPUT"

      # WiX 3 (candle/light) is preinstalled on windows-latest.
      - name: Build MSI
        shell: bash
        run: |
          candle -nologo -arch x64 \
            -dAppVersion=${{ steps.ver.outputs.numeric }} \
            -dExePath=target/release/hookecho.exe \
            -o dist/hookecho.wixobj packaging/windows/main.wxs
          light -nologo -o dist/HookEcho-x86_64.msi dist/hookecho.wixobj

      # Inno Setup 6 is preinstalled on windows-latest (not on PATH). pwsh avoids
      # Git-bash mangling of the /D switches.
      - name: Build setup EXE
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            /DAppVersion=${{ steps.ver.outputs.numeric }} `
            /DExeDir=${{ github.workspace }}\target\release `
            /Odist packaging\windows\hookecho.iss

      # The store manifests name a version and hash the artifacts we just built. Stamping them
      # here means the winget PR, the Homebrew tap push and the AUR push all start from files that
      # already match the release, instead of from hand-copied checksums. They are submission
      # inputs, so they ride out as an artifact rather than being committed back to main.
      - name: Stamp the store manifests
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          scripts/release/stamp-manifests.sh "${GITHUB_REF_NAME#v}" dist/HookEcho-setup-x86_64.exe

      - name: Upload the store manifests
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: store-manifests
          path: packaging/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hookecho-windows-x86_64
          path: |
            dist/hookecho-windows-x86_64.zip
            dist/HookEcho-x86_64.msi
            dist/HookEcho-setup-x86_64.exe

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        # bash, not the runner's default pwsh: `"$TAG"` is empty in PowerShell (it wants
        # `$env:TAG`), so this uploaded into tag "" and failed with "release not found" on
        # v0.12.0-beta.1 while every other platform, which runs bash, attached fine.
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: gh release upload "$TAG" dist/hookecho-windows-x86_64.zip dist/HookEcho-x86_64.msi dist/HookEcho-setup-x86_64.exe --clobber

      # Every push to main refreshes a rolling "latest" prerelease, so the Releases page always
      # carries current builds. Before this, artifacts only ever reached Releases on a tag push,
      # and 49 commits' worth of green runs published nothing.
      - name: Update rolling release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build (main)
          prerelease: false
          make_latest: true
          body: |
            Rolling build of `main` — rebuilt and re-published on every push, so this
            is always the newest code. Older releases are kept as drafts.
          files: |
            dist/hookecho-windows-x86_64.zip
            dist/HookEcho-x86_64.msi
            dist/HookEcho-setup-x86_64.exe

  macos:
    name: macOS app (experimental)
    runs-on: macos-latest
    # Waits only so the release exists before the upload at the end. `!cancelled()` because on a
    # push to main `create-release` is skipped, and a skipped dependency would otherwise skip the
    # build too.
    needs: create-release
    if: ${{ !cancelled() }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release -p hookecho

      # An app bundle by hand rather than cargo-bundle: it is a directory, a plist and an icon,
      # and a build-time dependency to write three files is not a trade worth making.
      - name: Assemble HookEcho.app
        run: |
          set -euo pipefail
          VERSION=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          APP=dist/HookEcho.app
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp target/release/hookecho "$APP/Contents/MacOS/hookecho"

          # Icons at every size the iconset wants, each drawn rather than resampled.
          ICONSET=dist/hookecho.iconset
          mkdir -p "$ICONSET"
          for px in 16 32 128 256 512; do
            ./target/release/hookecho --headless-icon "$ICONSET/icon_${px}x${px}.png" "$px"
            ./target/release/hookecho --headless-icon "$ICONSET/icon_${px}x${px}@2x.png" "$((px * 2))"
          done
          iconutil -c icns "$ICONSET" -o "$APP/Contents/Resources/hookecho.icns"

          cat > "$APP/Contents/Info.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleName</key><string>HookEcho</string>
            <key>CFBundleDisplayName</key><string>HookEcho</string>
            <key>CFBundleIdentifier</key><string>io.hookecho.HookEcho</string>
            <key>CFBundleVersion</key><string>$VERSION</string>
            <key>CFBundleShortVersionString</key><string>$VERSION</string>
            <key>CFBundleExecutable</key><string>hookecho</string>
            <key>CFBundleIconFile</key><string>hookecho</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>NSHighResolutionCapable</key><true/>
            <key>LSMinimumSystemVersion</key><string>11.0</string>
            <key>CFBundleURLTypes</key>
            <array>
              <dict>
                <key>CFBundleURLName</key><string>HookEcho deep link</string>
                <key>CFBundleURLSchemes</key><array><string>hookecho</string></array>
              </dict>
            </array>
          </dict>
          </plist>
          PLIST
          # The heredoc above is indented for readability; strip it back to a valid plist.
          sed -i '' 's/^          //' "$APP/Contents/Info.plist"
          plutil -lint "$APP/Contents/Info.plist"

      # Ad-hoc signature: there is no Apple Developer account behind this build, so Gatekeeper
      # will still ask. Signing at all is what keeps the bundle launchable on Apple silicon.
      - name: Sign and zip
        run: |
          set -euo pipefail
          codesign --force --deep --sign - dist/HookEcho.app
          codesign -dv dist/HookEcho.app
          ditto -c -k --keepParent dist/HookEcho.app dist/HookEcho-macos.zip

      - name: Smoke-test
        run: |
          ./dist/HookEcho.app/Contents/MacOS/hookecho --version
          ./dist/HookEcho.app/Contents/MacOS/hookecho --headless-icon /tmp/icon.png 64
          test -s /tmp/icon.png

      # The icon smoke proves the binary starts; this proves it renders — wgpu comes up on Metal,
      # a live volume is fetched and drawn off-screen, and the PNG reads back. It is the closest
      # thing to running the app that exists without Apple hardware to run it on.
      # `--basemap none` keeps the test off the tile servers; the size floor catches a valid PNG
      # of nothing. continue-on-error until it has been seen green: a red step nobody can debug
      # locally would block every release instead of reporting on one.
      - name: Render smoke
        continue-on-error: true
        run: |
          set -euo pipefail
          ./dist/HookEcho.app/Contents/MacOS/hookecho --snapshot /tmp/smoke.png KTLX \
            --basemap none --size 256
          test "$(stat -f%z /tmp/smoke.png)" -ge 5000

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HookEcho-macos
          path: dist/HookEcho-macos.zip

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: gh release upload "$TAG" dist/HookEcho-macos.zip --clobber

      - name: Update rolling release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build (main)
          prerelease: false
          make_latest: true
          body: |
            Rolling build of `main` — rebuilt and re-published on every push, so this
            is always the newest code. Older releases are kept as drafts.
          files: dist/HookEcho-macos.zip

  android:
    name: Android APK (arm64-v8a)
    runs-on: ubuntu-latest
    # Waits only so the release exists before the upload at the end. `!cancelled()` because on a
    # push to main `create-release` is skipped, and a skipped dependency would otherwise skip the
    # build too.
    needs: create-release
    if: ${{ !cancelled() }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (+ android target)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          # Named explicitly. The action's default package list has included `tools`, which Google
          # no longer serves; sdkmanager answers "Failed to find package 'tools'" and exits 1,
          # which failed the whole Android job. platform-tools is all the build needs from it;
          # platforms and build-tools come from the runner image / Gradle.
          packages: platform-tools

      - name: Set up Android NDK
        uses: nttld/setup-ndk@v1
        id: ndk
        with:
          ndk-version: r26d

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      # Build the Rust cdylib into the Gradle jniLibs dir, then package the APK. Release build type,
      # signed from repo secrets when present (a fork without them falls back to the debug key).
      - name: Build libhookecho.so
        env:
          ANDROID_NDK_HOME: ${{ steps.ndk.outputs.ndk-path }}
        run: cargo ndk -t arm64-v8a -o android/app/src/main/jniLibs build --release --lib -p hookecho

      # Stable signature across runs (phones reject updates whose signature changes). Absent
      # secrets (e.g. forks) fall back to a per-run debug key — still installable, not updatable.
      - name: Materialize signing keystore
        env:
          KS_B64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
        run: |
          if [ -n "$KS_B64" ]; then
            echo "$KS_B64" | base64 -d > "$RUNNER_TEMP/hookecho.p12"
            echo "HOOKECHO_KEYSTORE=$RUNNER_TEMP/hookecho.p12" >> "$GITHUB_ENV"
          fi

      # AAB alongside the APK: the Play track needs a bundle, sideloaders need the APK.
      - name: Assemble APK + AAB
        working-directory: android
        env:
          HOOKECHO_KEYSTORE_PASS: ${{ secrets.ANDROID_KEYSTORE_PASS }}
        run: ./gradlew assembleRelease bundleRelease --no-daemon

      - name: Rename APK
        run: |
          # Without the keystore secrets (forks) AGP emits app-release-unsigned.apk instead.
          apk=android/app/build/outputs/apk/release/app-release.apk
          [ -f "$apk" ] || apk=android/app/build/outputs/apk/release/app-release-unsigned.apk
          cp "$apk" HookEcho-arm64-v8a.apk
          cp android/app/build/outputs/bundle/release/app-release.aab HookEcho-arm64-v8a.aab

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HookEcho-arm64-v8a.apk
          path: HookEcho-arm64-v8a.apk

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: HookEcho-arm64-v8a.aab
          path: HookEcho-arm64-v8a.aab

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: gh release upload "$TAG" HookEcho-arm64-v8a.apk --clobber

      # Every push to main refreshes a rolling "latest" prerelease, so the Releases page always
      # carries current builds. Before this, artifacts only ever reached Releases on a tag push,
      # and 49 commits' worth of green runs published nothing.
      - name: Update rolling release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build (main)
          prerelease: false
          make_latest: true
          body: |
            Rolling build of `main` — rebuilt and re-published on every push, so this
            is always the newest code. Older releases are kept as drafts.
          files: HookEcho-arm64-v8a.apk

  # The image people actually run the server from. Built on every push to main so `:latest`
  # tracks the rolling release, and tagged with the version on a `v*` push.
  docker:
    name: Container image (ghcr.io)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Lowercased because the registry rejects capitals in a repository name, and GitHub
      # usernames may have them.
      - name: Build
        id: build
        run: |
          image="ghcr.io/$(echo '${{ github.repository }}' | tr 'A-Z' 'a-z')"
          docker build -t "$image:latest" .
          case "${{ github.ref }}" in
            refs/tags/v*) docker tag "$image:latest" "$image:${GITHUB_REF#refs/tags/v}";;
          esac
          echo "image=$image" >> "$GITHUB_OUTPUT"

      # A container that can't start is worse than no container; find out here, not in someone's
      # compose file.
      - name: Smoke-test the image
        run: docker run --rm --entrypoint hookecho ${{ steps.build.outputs.image }}:latest --version

      - name: Push
        run: docker push --all-tags ${{ steps.build.outputs.image }}

  # Only the current build should be public. Everything else gets demoted to a draft:
  # ... except prereleases, which are checkpoints people are asked to go and test: an alpha that
  # goes invisible on the next push to main is an alpha nobody can install. They stay until the
  # release they lead to ships, and are cleaned up by hand then.
  # still on the Releases page for anyone with push access, assets intact, but invisible
  # (and undownloadable) to the public. Deleting would lose the binaries; drafts don't.
  # The git tags stay public either way — they're repo history, not release pages.
  demote-old:
    name: Hide superseded releases
    needs: [appimage, windows, android]
    runs-on: ubuntu-latest
    steps:
      - name: Draft every release except the current one
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          REF: ${{ github.ref }}
        run: |
          # Keep the rolling build; on a tag push keep that tag's release too.
          keep="latest"
          case "$REF" in refs/tags/*) keep="$keep ${REF#refs/tags/}";; esac
          gh api "repos/$REPO/releases" --paginate \
            --jq '.[] | select(.draft == false and .prerelease == false) | "\(.id) \(.tag_name)"' |
          while read -r id tag; do
            case " $keep " in *" $tag "*) continue;; esac
            echo "drafting $tag (id $id)"
            gh api -X PATCH "repos/$REPO/releases/$id" -F draft=true >/dev/null
          done
